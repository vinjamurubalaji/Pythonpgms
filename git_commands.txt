ls
cd
git init
git remote add origin "http link"
git remote -v

--to add files--
git add .

commit files
git commit -m "enter the comments"

push
git push origin master

creating new branch 
git checkout -b branchname

commiting new branch creation to github
git push --set-upstream origin branchname